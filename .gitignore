# DeerFlow docker image cache
docker/.cache/
# oh-my-claudecode state
.omc/
# OS generated files
.DS_Store
*.local
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv
venv/

# Environment variables
.env

# Configuration files
config.yaml
mcp_config.json
extensions_config.json

# IDE
.idea/
.vscode/

# Coverage report
coverage.xml
coverage/
.deer-flow/
.claude/
skills/custom/*
# text-to-cad integration skill is committed (not local-only like other custom skills)
!skills/custom/cad-modeling/
# engineering-drawing-platform skills (2D 煤矿设计 + 化工 P&ID) — committed, not local-only
!skills/custom/mine-design/
!skills/custom/chemplant-design/
logs/
log/
debug.log

# Local git hooks (keep only on this machine, do not push)
.githooks/

# pnpm
.pnpm-store
sandbox_image_cache.tar

# ignore the legacy `web` folder
web/

# Deployment artifacts
backend/Dockerfile.langgraph
config.yaml.bak
.playwright-mcp
/frontend/test-results/
/frontend/playwright-report/
.gstack/
.worktrees
# Added by code-review-graph
.code-review-graph/

# RAGFlow runtime data
docker/ragflow/
_workspace_temp/

# License keys (never commit)
tools/license/private_key.pem
*.lic
.claude/skills/gstack/

# === Runtime data / build artifacts / local business docs (never commit) ===
# Runtime user data (knowledge bases, uploads)
backend/data/users/
# Build artifacts (offline deployment bundles)
/eai-flow-offline-*.tar.gz
/eai-flow-offline-*/
# Cloned external repos / large local dirs
/text-to-cad-main/
/科技控制中心仪表盘-3/
# Local business documents (root only) — docx/pdf/md
/*.docx
/*.pdf
/开发日志.md
backend/packages/harness/deerflow/community/serper/__pycache__/
nul
