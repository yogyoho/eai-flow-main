# 角色自定义 overlay —— 覆盖 permissions.yaml 中的内置角色定义，或新增自定义角色。
# 合并规则：同 code 整体覆盖内置定义；自定义角色只存在于本文件。
# 删除内置角色 = 加入 disabled_roles 列表（不移除 permissions.yaml 中的定义）。
# 本文件由角色管理 UI 写透维护（RoleService.create/update/delete/copy），也可手工编辑。
# 变更通过 mtime 热重载即时生效（下一请求）。
roles: {}
disabled_roles: []
