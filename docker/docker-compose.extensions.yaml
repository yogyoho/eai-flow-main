# DeerFlow Extensions Services
# Usage: docker compose -f docker-compose.yaml -f docker-compose.extensions.yaml up -d
#
# Services:
#   - postgres-ext: Extensions PostgreSQL (auth, knowledge, etc.)
#   - collab: Real-time collaboration WebSocket server (Hocuspocus)

services:
  # ── Extensions PostgreSQL ──────────────────────────────────────────────
  postgres-ext:
    image: postgres:16-alpine
    container_name: eai-flow-postgres-ext
    environment:
      - POSTGRES_USER=${POSTGRES_EXT_USER:-agentflow}
      - POSTGRES_PASSWORD=${POSTGRES_EXT_PASSWORD:-agentflow123}
      - POSTGRES_DB=${POSTGRES_EXT_DB:-agentflow}
      - POSTGRES_HOST_AUTH_METHOD=md5
      - TZ=Asia/Shanghai
    volumes:
      - postgres-ext-data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_EXT_PORT:-5432}:5432"
    networks:
      - eai-flow-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_EXT_USER:-agentflow} -d ${POSTGRES_EXT_DB:-agentflow}"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  # ── Collaboration WebSocket Server (Hocuspocus) ──────────────────────
  # Real-time document collaboration via Yjs/Hocuspocus over WebSocket
  collab:
    build:
      context: ../backend/collab-server
      dockerfile: Dockerfile
    container_name: eai-flow-collab
    # EAI-CUSTOM (bug-3209): 发布 8002 到 host——前端 dev 拓扑(:3000/:4000)的
    # useCollab 直连 ws://localhost:8002(不经 nginx),无映射则协作编辑器永不 synced。
    ports:
      - "8002:8002"
    depends_on:
      postgres-ext:
        condition: service_healthy
    environment:
      - COLLAB_PORT=8002
      - DATABASE_URL=postgresql://${POSTGRES_EXT_USER:-agentflow}:${POSTGRES_EXT_PASSWORD:-agentflow123}@postgres-ext:5432/${POSTGRES_EXT_DB:-agentflow}
    env_file:
      - ../.env
    networks:
      - eai-flow-net
    restart: unless-stopped

volumes:
  postgres-ext-data:

networks:
  eai-flow-net:
    external: true
    name: eai-docker_eai-flow-net
