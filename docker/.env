# Docker环境变量配置
# 使用方法: docker compose --env-file .env -f docker-compose-dev.yaml up --build

# Windows路径
HOME=C:/Users/admin
USERPROFILE=C:/Users/admin

# DeerFlow根目录
DEER_FLOW_ROOT=D:/eai/eai-flow-main

# PNPM store
PNPM_STORE_PATH=C:/Users/admin/AppData/Local/pnpm/store

# UV超时设置 (秒) - 网络慢时增大
UV_HTTP_TIMEOUT=600

# 可选镜像加速
# APT_MIRROR=mirrors.aliyun.com
# UV_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/
# NPM_REGISTRY=https://registry.npmmirror.com

# gateway 镜像构建 extras（docker-compose.yaml build arg）。EAI-CUSTOM 2026-08-29:
# 核心 DB 已切 postgres（config.yaml database.backend），postgres extra 装
# psycopg/langgraph-checkpoint-postgres；memory-zh 装 jieba。与根 .env 的
# UV_EXTRAS（dev 容器运行时 uv sync 用）保持一致。
UV_EXTRAS=postgres,memory-zh

# ─────────────────────────────────────────────────────────────────────────
# 网络名称 (所有 compose 文件共享)
# ─────────────────────────────────────────────────────────────────────────
NETWORK_NAME=eai-flow-net

# ─────────────────────────────────────────────────────────────────────────
# Extensions PostgreSQL
# ─────────────────────────────────────────────────────────────────────────
POSTGRES_EXT_USER=agentflow
POSTGRES_EXT_PASSWORD=agentflow123
POSTGRES_EXT_DB=agentflow
POSTGRES_EXT_PORT=5432

# ─────────────────────────────────────────────────────────────────────────
# Business PostgreSQL (统一)
# ─────────────────────────────────────────────────────────────────────────
BUSINESS_DB_USER=business
BUSINESS_DB_PASSWORD=business123
BUSINESS_DB_PORT=15433

# ─────────────────────────────────────────────────────────────────────────
# RAGFlow
# ─────────────────────────────────────────────────────────────────────────
RAGFLOW_IMAGE=infiniflow/ragflow:v0.25.3
RAGFLOW_WEB_PORT=9381
RAGFLOW_PORT=9380
ELASTIC_PASSWORD=ragflow123
MYSQL_ROOT_PASSWORD=ragflow123
MYSQL_DATABASE=ragflow
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
ES_PORT=9200
MYSQL_PORT=3306
REDIS_PORT=6379
REDIS_PASSWORD=ragflow123
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
