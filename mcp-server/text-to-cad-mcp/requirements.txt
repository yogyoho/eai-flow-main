build123d>=0.10
mcp>=1.2
trimesh>=4.0
# Vendored text-to-cad engine (MIT) — editable install of cadpy (assembly/ref/analysis)
--editable ./cad-skill/packages/cadpy
# ponytail: deferred to snapshot phase — snapshot renders via Playwright+Chromium
# playwright>=1.40
