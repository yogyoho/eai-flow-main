# Phase 4 — P1 前置批 + 批量入库快赢 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清掉 1000 份批量入库的三件 P1 前置（服务端 defer_parse 真生效 / host pytest env 毒化治理 / 技能层 geo 16 红清零）+ 两个 CLI 快赢（CSV 预检 / scan 容错）。

**Architecture:** defer_parse = 上传 Form 字段条件跳过后台任务（状态机零改动，行停 uploaded；复用既有单体 /parse 触发）；env 毒化 = backend/tests/conftest.py 模块体早期 `os.environ.setdefault` 中和（单卡点，早于一切测试模块导入，`override=False` 语义使 setdefault 即屏蔽）；技能 16 红 = 测试夹具对齐四个已文档化门禁硬化 commit 的行为（bug-3049/3036/3059 与 per-mineral stderr），非改测试凑绿——断言对象是硬化后的正式契约。

**Tech Stack:** FastAPI Form + argparse CLI + pytest conftest/Makefile + python-docx 夹具。

**Spec:** 上游 `2026-09-03-geo-batch-ingest-cli-design.md`（§3/§7 的 defer_parse 与风险①③在本期闭环）；本计划自含（recon wf_696acb45-624 三路实证）。

**约定（全任务通用）：** pathspec 提交；并发会话活跃（skills/geological-report 09-02 后无新提交、工作区净——动手前仍 `git log -1` 复核）；后端测试 `cd backend && PYTHONPATH=. uv run pytest ...`；EAI-CUSTOM 注释；Do NOT restart docker（T8 统一）。

---

### Task 1: 服务端 defer_parse（上传 Form 字段 + 条件跳过）

**Files:**
- Modify: `backend/app/extensions/geo_samples/routers.py`（upload 端点：Form 字段 + 条件跳过；run_id 契约）
- Test: `backend/tests/test_geo_sample_bank_compile.py`（追加）

**行为契约**：`defer_parse: bool = Form(False)`——true 时仅 put_raw + create_document（status=uploaded），**不** create_run、**不** add_task、响应 `{"document": …}`（**省略 run_id 键**——CLI `.get` 已容忍，前端 upload.mutate onSuccess 不读 run_id，types.ts uploadDocument 返回类型改 `run_id?: string`）；false 行为逐字节不变。

- [ ] **Step 1: 写失败测试**（追加；mock 模式照抄既有 upload 测试——grep `documents/upload` 于测试文件定位）

```python
@pytest.mark.asyncio
async def test_upload_defer_parse_skips_run(monkeypatch):
    """defer_parse=true：落行+raw 上传，但不 create_run、不调 run_parse。"""
    # mock 面与既有 upload 测试同款（crud.get_document_by_report_id→None、
    # find_duplicate→None、storage.put_raw、crud.create_document 捕获行、
    # crud.create_run→AsyncMock 断言未被调用、crud.has_running_run→False）
    # FormData 带 defer_parse="true"
    # 断言：doc.status=="uploaded" 且 raw_uri 在；create_run.assert_not_awaited()；
    #       响应 JSON 无 "run_id" 键
```

（既有 upload 测试已存在则在其旁新增——mock 面复制现成，仅加 create_run 未调用断言与 defer 表单字段。若既有测试无 upload 行为级用例，则本测试为首 个，须同时覆盖 false 路径回归：`test_upload_immediate_parse_runs`——defer_parse 缺省 → create_run 被调 + BackgroundTasks.add_task 断言（BackgroundTasks 用真实例，断言 `bg.tasks` 长度 1）。）

- [ ] **Step 2: 确认失败** → **Step 3: 实现**——routers.py upload：

```python
    defer_parse: bool = Form(False),   # :60-65 Form 区追加
    ...
    # （原 :94-95 无条件段改为：）
    if not defer_parse:
        run = await crud.create_run(db, doc.id, "parse")
        background.add_task(service.run_parse, db, doc.id, run.id)
```

响应构造改条件：

```python
    resp = {"document": doc.model_dump()}
    if not defer_parse:
        resp["run_id"] = run.id
    return resp
```

（run 变量仅在非 defer 分支存在——注意分支内取值。types.ts `uploadDocument` 返回类型 `run_id: string` → `run_id?: string`。）

- [ ] **Step 4: 全绿**（新测试 + geo 系 -q 零回归）→ **Step 5: Commit**

```bash
git add backend/app/extensions/geo_samples/routers.py backend/tests/test_geo_sample_bank_compile.py frontend/src/extensions/geo-samples/types.ts
git commit -m "feat(geo-samples): server-side defer_parse on upload (batch-cli P4 T1)" -- backend/app/extensions/geo_samples/routers.py backend/tests/test_geo_sample_bank_compile.py frontend/src/extensions/geo-samples/types.ts
```

---

### Task 2: CLI defer_parse 透传 + 契约同步

**Files:**
- Modify: `tools/eai.py`（upload_one data 字段 + import 的 --defer-parse help/docstring）
- Test: `backend/tests/test_eai_cli.py`（追加）

- [ ] **Step 1: 写失败测试**

```python
def test_upload_one_defer_parse_passthrough(tmp_path):
    """defer_parse=True → multipart data 含 defer_parse=true 且成功后不调 parse。"""
    f = tmp_path / "a.docx"; f.write_bytes(b"x")
    posts = []

    class FakeSess:
        def post(self, path, files=None, data=None, **kw):
            posts.append((path, data))
            if path.endswith("/upload"):
                return httpx.Response(200, json={"document": {"id": "d1", "report_id": data["report_id"]}},
                                      request=httpx.Request("POST", path))
            return httpx.Response(409, json={"detail": "解析任务已在跑"}, request=httpx.Request("POST", path))

    row = {"file_name": "a.docx", "report_id": "gsb-kc-cu-0001", "stage": "exploration",
           "mineral": "copper", "region": "", "confidence": "auto"}
    result = eai.upload_one(FakeSess(), str(f), row, defer_parse=True)
    assert result["report_id"] == "gsb-kc-cu-0001"
    up_data = posts[0][1]
    assert up_data["defer_parse"] == "true"
    assert len([p for p, _ in posts if p.endswith("/parse")]) == 0   # 不再发幂等 parse
```

- [ ] **Step 2: 确认失败** → **Step 3: 实现**——upload_one 的 data 构造处按 defer_parse 追加：

```python
    if defer_parse:
        data["defer_parse"] = "true"
```

并把 `--defer-parse` help 与模块头 docstring 契约锚点同步为真语义：「服务端 defer：行停 uploaded，后续用 `gsb parse-batch` 或单体 /parse 触发」。

- [ ] **Step 4: 全绿** → **Step 5: Commit**

```bash
git add tools/eai.py backend/tests/test_eai_cli.py
git commit -m "feat(cli): defer_parse passthrough to upload endpoint (batch-cli P4 T2)" -- tools/eai.py backend/tests/test_eai_cli.py
```

---

### Task 3: 批量 parse 触发端点（defer 行的受控启动）

**Files:**
- Modify: `backend/app/extensions/geo_samples/routers.py`（POST /documents/parse-batch）
- Test: `backend/tests/test_geo_sample_bank_compile.py`（追加）

**契约**：`POST /documents/parse-batch`（可选 query `limit: int = Query(5, ge=1, le=20)`）——按 created_at asc 取 status=uploaded 的前 N 行，逐行 create_run+add_task（**N≤20 天然并发闸**，替代信号量）；返回 `{"scheduled": N, "ids": [...]}`；零行 → `{"scheduled": 0, "ids": []}`。sweep 先行（同 parse 端点惯例）。

- [ ] **Step 1: 写失败测试**（直测端点函数，mock 模式同 T1）

```python
@pytest.mark.asyncio
async def test_parse_batch_schedules_uploaded_rows(monkeypatch):
    # crud.list_uploaded(db, limit) → 2 行；create_run 依次返回 r1/r2；
    # 断言 background.add_task 调 2 次且路径参数为 run.id/doc.id；响应 scheduled==2
```

（crud 追加 `list_uploaded(db, limit)`：status=="uploaded" asc——同 list_reviewed 模式。）

- [ ] **Step 2: 确认失败** → **Step 3: 实现**（routers + crud.list_uploaded）→ **Step 4: 全绿** → **Step 5: Commit**

```bash
git add backend/app/extensions/geo_samples/routers.py backend/app/extensions/geo_samples/crud.py backend/tests/test_geo_sample_bank_compile.py
git commit -m "feat(geo-samples): parse-batch endpoint for deferred uploads (batch-cli P4 T3)" -- backend/app/extensions/geo_samples/routers.py backend/app/extensions/geo_samples/crud.py backend/tests/test_geo_sample_bank_compile.py
```

---

### Task 4: host pytest env 毒化治理（conftest 单卡点中和）

**Files:**
- Modify: `backend/tests/conftest.py`（line 20 后插入中和块）
- Modify: `backend/Makefile`（test 目标 recipe 同步前缀——双保险，覆盖 -p no:cacheprovider 等绕过 conftest 的调用形态）

**中和块**（conftest.py line 19 sys.path 插入之后、任何 deerflow 导入之前——模块体执行即生效，早于 collection）：

```python
# EAI-CUSTOM (geo-batch-cli P4): host pytest env 毒化治理——仓库根 .env 是容器部署
# env 文件（DEER_FLOW_* 全是 /app 容器路径），harness app_config 与 extensions config
# 的模块级 load_dotenv(override=False) 会把它吸进 host 测试进程（~190 假失败 +
# requires_llm 真跑 LLM 挂死）。setdefault 空串即屏蔽（override=False 不覆盖已存在键）。
# 容器内运行时这些变量由 compose 注入真值，setdefault 不覆盖。
for _k in (
    "DEER_FLOW_DEV_MODE", "DEER_FLOW_HOME", "DEER_FLOW_CONFIG_PATH",
    "DEER_FLOW_EXTENSIONS_CONFIG_PATH", "DEER_FLOW_DOCKER_SOCKET",
    "DEER_FLOW_REPO_ROOT", "DEER_FLOW_HOST_BASE_DIR", "DEER_FLOW_HOST_SKILLS_PATH",
    "OPENAI_API_KEY",
):
    os.environ.setdefault(_k, "")
```

（conftest 顶部确认 `import os` 在场；插入点必须在 line 18-19 sys.path 操作之后、:33-40 sys.modules mock 之前。OPENAI_API_KEY 置空使 requires_llm 类测试回到 skipif 跳过路径。）

- [ ] **Step 1:** conftest 插入中和块（无测试先行——本任务是测试基建自身，验证=门禁结果对比）
- [ ] **Step 2:** Makefile test 目标 recipe 尾部同步追加（双保险覆盖 `make test` 明确语义）：

```make
	PYTHONUTF8=1 DEER_FLOW_DEV_MODE= DEER_FLOW_HOME= DEER_FLOW_CONFIG_PATH= DEER_FLOW_EXTENSIONS_CONFIG_PATH= DEER_FLOW_DOCKER_SOCKET= DEER_FLOW_REPO_ROOT= DEER_FLOW_HOST_BASE_DIR= DEER_FLOW_HOST_SKILLS_PATH= OPENAI_API_KEY= uv run pytest -m "not live" --ignore=tests/blocking_io tests/ -v
```

- [ ] **Step 3: 验证**——`cd backend && make test 2>&1 | tail -5`：期望毒化类失败（config/skills/MCP 路径类 ~190 个）消失；剩余失败与 T8 归因清单对齐（postgres checkpointer/POSIX 权限/symlink/技能层夹具漂移——技能层红由 Task 5/6 清）。**注意**：make test 全程 ~15 分钟，可先 `uv run pytest tests/test_extensions_config.py tests/test_client.py -q` 快速抽验毒化消失再放全量。
- [ ] **Step 4: Commit**

```bash
git add backend/tests/conftest.py backend/Makefile
git commit -m "fix(test-infra): neutralize root .env poisoning for host pytest (P4 env)" -- backend/tests/conftest.py backend/Makefile
```

---

### Task 5: 技能层 geo 测试红清零（16 红 = 夹具对齐四处门禁硬化）

**Files:**
- Modify: `backend/tests/test_geo_progress.py`（6F——bug-3049 手动 mark VERIFIED 禁用后的 gate 流对齐）
- Modify: `backend/tests/test_geo_controller_build.py`（2F——stderr 断言 `in` 化）
- Modify: `backend/tests/test_geological_report_bug2223.py`（3F——夹具真节标题）
- Modify: `backend/tests/test_geological_report_e2e_full.py`（5E——规范名复用 + 真节标题，**一次双修**）
- 无源码改动——四个硬化 commit（7aa135200/61d03175d/2ad95d0d6/10cefae98）是正式修复，测试对齐其契约

**修法（recon 已逐例归因）**：
1. **test_geo_progress 6F**：bug-3049 后手动 mark VERIFIED 一律拒绝（progress.py:253-257）——6 个用例走「手动 mark VERIFIED」旧流程 → 改为 `progress.py gate` 真跑流或直接断言「手动 mark VERIFIED 被拒 rc=1」（按每用例原意图选择：测状态机转移合法性的改走 gate；专测拒绝的保留改断言）。**先跑 -v 收敛 6 个确切失败点名再逐个对齐。**
2. **test_geo_controller_build 2F**：`assert r.stderr.startswith("[build] 错误:")` → stderr 首行已是「[build] 矿种基线缺失: …」（resolve_targets 回退警告，P2 T4 正式行为）——改 `assert "[build] 错误:" in r.stderr`（或断言含「矿种基线缺失」与「错误」两段）。
3. **test_geological_report_bug2223.py 3F**：夹具骨架节标题写「…{no} 小节」占位 → 目录覆盖门比对节标题失败。修法：夹具骨架从 stage toc 渲染真实节标题（标题文本与 toc 条目一致）；注意 e2e 修名门后连锁撞覆盖门——**两文件同一 commit 双修**。
4. **test_geological_report_e2e_full.py 5E**：交付名门 FAIL（夹具朴素拼接 vs bug-3036 去重规则）——夹具改调 `build_output.expected_deliverable_name(project_name, stage)` 生成期望名（脚本已有函数，直接 import 或复制去重逻辑）；同步修 `_skeleton` 真节标题（连锁预防）。

- [ ] **Step 1:** 四文件逐个跑 `-v` 收敛确切失败清单（16 项点名）→ **Step 2:** 按上述修法逐文件对齐（改测试断言/夹具，**不回退任何源码硬化**——bug-3049/3036 是正式修复）→ **Step 3:** 四文件 + v2_scripts 全绿：

```bash
cd backend && PYTHONPATH=. PYTHONUTF8=1 uv run pytest tests/test_geo_progress.py tests/test_geo_controller_build.py tests/test_geological_report_bug2223.py tests/test_geological_report_e2e_full.py tests/test_geological_report_v2_scripts.py -q
```

- [ ] **Step 4: Commit**

```bash
git add backend/tests/test_geo_progress.py backend/tests/test_geo_controller_build.py backend/tests/test_geological_report_bug2223.py backend/tests/test_geological_report_e2e_full.py
git commit -m "test(geo-skill): align fixtures/assertions with hardened gates (bug-3049/3036/3059 contracts, 16 red -> 0) (P4)" -- <四测试文件>
```

---

### Task 6: CLI 快赢——CSV 预检（D2）+ scan 容错（D3）

**Files:**
- Modify: `tools/eai.py`
- Test: `backend/tests/test_eai_cli.py`（追加）

**D2 预检**：`cmd_gsb_import` 读 CSV 后、上传循环前加 `preflight_errors = [...]`——逐行校验 report_id slug 正则 `^[a-z0-9][a-z0-9\-_][a-z0-9\-_]*$`（2-128）、stage/mineral ∈ ALLOWED 集合（CLI 内置同款集合常量）、file 列非空；有错 → 打印逐行错误 + rc=1 不起任何上传（整列typed错在 900 round-trips 前发现）。
**D3 容错**：`_remote_suggest` 的 `raise_for_status()` 包 try/except httpx.HTTPError → 该文件计 `needs-review`（stage/mineral/region 置 None）+ stderr 警告计数，批次继续；汇总行加 `解析异常 K`。

- [ ] **Step 1: 写失败测试**：`test_gsb_import_preflight_rejects_bad_vocab`（CSV 一行 mineral=uranium → rc=1 且零上传调用）；`test_gsb_scan_tolerates_suggest_500`（fake suggest 500 → 该行 needs-review 不中断）
- [ ] **Step 2: 确认失败** → **Step 3: 实现** → **Step 4: 全绿** → **Step 5: Commit**

```bash
git add tools/eai.py backend/tests/test_eai_cli.py
git commit -m "feat(cli): CSV pre-flight validation + scan per-file fault tolerance (batch-cli P4 D2/D3)" -- tools/eai.py backend/tests/test_eai_cli.py
```

---

### Task 7: 端到端验收 + 全量门禁

- [ ] **Step 1:** 后端 `make lint && make test`（env 中和后毒化类假失败应消失；剩余与 T8 归因清单对齐）；前端三闸
- [ ] **Step 2:** defer e2e：CLI `import --defer-parse` 上传 2 份 → DB status=uploaded → `POST /documents/parse-batch?limit=2` → 轮询至 parsed → compile 链回归
- [ ] **Step 3:** CLI 两阶段回归（scan→import→409 断点，同 Phase 3 T8 七步）
- [ ] **Step 4:** 汇总报告（16 红清零证据 / defer 链 / 门禁数字）

---

## 完成判据（验收）

1. geo 后端 5 文件 + CLI 全绿；`make test` 毒化类假失败清零（剩余归因清单缩短为环境真债）
2. 技能层 16 红清零（四文件 + v2_scripts 零回归）
3. defer 链实测：defer 上传 → uploaded 滞留 → parse-batch 受控启动 → parsed
4. CLI 预检拒绝坏词表 CSV 于零上传前；scan 单文件 500 不炸批次

## 明确不做（随需）

批量 parse 的并发信号量（N≤20 行业上限够 V1）；JSON body 版 suggest-id；SESSION_FILE 会话持久化；depth_targets 真实基线数据（由真实批量入库自然产出——Phase 4 后置验收）。

## 关键风险备忘

1. **并发会话协调**：skills/geological-report 测试与脚本动手前 `git log -1` 复核；本计划全部为测试文件对齐、零源码回退（禁回退 bug-3049/3036/3059 硬化）
2. **argparse required+default 互斥坑**（bug-3083）：条件 required 用 `required="EAI_PASSWORD" not in os.environ` 惯用法
3. **defer 行滞留窗口**：gateway 重启丢 BackgroundTasks——defer 行 uploaded 滞留由 gsb status uploaded 计数暴露（已有），parse-batch 兜底
4. **e2e 连锁修**：名门与覆盖门同 commit 双修，防「修一门红一门」误判
