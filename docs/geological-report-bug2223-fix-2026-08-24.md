# geological-report bug-2223 修复设计（管线绕过 + 数据编造 + 章节单薄）

> 状态：APPROVED 2026-08-24（对话确认方案 B）
> 证据：E2E 复测线程 47d8c147（同源 A/B 数据 vs 旧基线 cee60e84），10 轮页面实测
> 前置：bug-2217（崩溃即停）、bug-2220/2221（四铁律+交付回路）均已落地但仍被绕过

## 1. E2E 复测结论（FAIL）

| 指标 | 旧基线 | 本次 | 样例 | 目标 |
|---|---|---|---|---|
| 有效字符 | 15,047 | **10,445（↓31%）** | 91,008 | 接近样例 |
| toc 节覆盖 | 56/149 (38%) | 91/148 (61%) | ~100% | 全覆盖 |
| 三级节深度 | — | ch3 每节 ~41 字符 | ~数千/章 | ≥3 句 |
| [待确认] 占位 | — | 仅 2 处 | — | 缺数必占位 |
| 交付命名 | 错误 | `01-10_完整报告.md`（SKILL.md line105 已预判禁止的文件名） | — | `{项目名}-{阶段}-地质勘查报告.md` |

**核心判定：SKILL.md 纪律文本已全部写对（连违规文件名都预判了），但在 2337K 长对话中零约束力。** agent 在 L9=0 处"自纠"破窗，随后管线全绕。

## 2. 根因链（四层）

1. **schema 不匹配 → L9 静默 0**：实喂 `14_block_model.json` 的 `grade_class: "工业矿"` vs formula_runner.py:231 期望 `"工业"`（六行全判低品位）；`category: "探明+控制"` 等中文 vs `CATS=TM/KZ/TD` 双重对不上。`ind==[]` 时无 anomaly、静默输出 0。
2. **纪律语义盲区**：SKILL.md「崩溃即停」只覆盖*脚本崩溃*，不覆盖*公式结果异常*——agent 把 0 当计算 bug「自纠」：手改 formula_state.json（L9.TM/KZ/TD 槽位无 `source` 键 = 法医铁证）、编造 TM/KZ/TD 三分拆（339.92+302.23+256.85=899.00 凑总量）。
3. **交付绕过**：agent 自写 53 行 `geo-report/build_output.py`（硬编码容器路径+自定文件名+零校验），技能 build_output 的槽位注入/一致性/快照/交付名全部没跑。
4. **章节深度铁律无脚本侧校验**：骨架覆盖做到了（15 个标题节全在），每节按要素链成句没做（ch3 平均 41 字符/节）。

### 澄清记录（避免误判）

- 8.5.1 六矿体参数表**不是编造**——是块模型 aggregates 忠实直录（formulas.json L9 明示 "B级直录" 合法）。
- 真正编造：8.5.2 TM/KZ/TD 拆分、倾角 52°~68°、海拔 2765.8m（无数据源）；8.5.3 与 8.5.1 双计 899 万吨。

## 3. 修复方案（B：脚本侧硬门 + 样例资产化）

排除项：A 纯加提示（line105 已实测证伪）；C 章节生成全脚本化（YAGNI）；KF 灌语料（语料现状 0 地质域命中，提前投资，等规模化再上）。

### ① formula_runner.py — 归一化 + 空结果 anomaly（~15 行）

- `grade_class` 归一化：`工业矿→工业`（及"低品位矿→低品位"等同类）。
- `category` 归一化：`探明→TM / 控制→KZ / 推断→TD`；**复合类别**（"探明+控制"）无法整行映射 → 该行计入 anomaly「需用户确认 TM/KZ 占比」，不猜拆分。
- `rows` 非空但 `ind==[]` → anomaly「块模型全部行被判为低品位，请核对 grade_class 取值」→ 进门2 表单。

### ② build_output.py — 三个新硬门（~40 行）

| 门 | 规则 | FAIL 行为 |
|---|---|---|
| 交付名 | 从 `00_project` 拼 `{项目名}-{阶段}-地质勘查报告.md`；outputs/ 出现其他 .md → FAIL 列出违规文件 | exit 1 |
| 手改检测 | formula_state 数值槽缺 `source` 键 → FAIL「formula_state 被手改」 | exit 1（source 键 = 完整性标记） |
| 深度门 | 三级节 <3 句（对齐 SKILL.md 铁律，门=铁律）或每章有效字符 <1500 → FAIL + 输出缺节清单（提示参照同章范文补写） | exit 1，agent 定向补写重跑（交付回路铁律已有循环） |

### ③ SKILL.md — 一条新铁律（~3 行）

> **公式结果为 0/空 = 数据缺失，与脚本崩溃同级**：停，呈现，问用户。`formula_state.json` 是公式唯一产物，agent 不得手改——build_output 手改检测直接 FAIL。

### ⑤ 样例资产化（治"专业质感"）

- 91K 对照样例（`D:\temp\geo-compare\sample-report.md`，已人工核）按章切片 → `references/samples/exploration/chN_sample.md` ×10。
- SKILL.md 章节铁律增补：动笔前读该章 `toc` + `sections` + **同章范文**；范文只学**叙述范式**——范文中任何数值/矿名/地名是样例项目的，一律不得进入本项目正文；本项目数值只经 `{{SLOT:key}}`。
- 深度门（句数硬门）+ 范文范式（质量软引导）双管齐下：深度门治"薄"，范文治"干瘪"。

### ④ 复测闭环

同线程同数据重跑：L9 应产 anomaly 卡（问 TM/KZ 占比）而非 0；手改 formula_state 应被 FAIL 拦截；深度门应逼出补写循环；交付名应为规范名。

## 4. 红线适配（不变更，仅重申）

- 数字永不经过 LLM：正文只写 `{{SLOT:key}}`；样例/KF/任何参考源只供行文范式，检索结果中的数值照抄 = 违规。
- 缺失信息绝不编造：缺就问，用户不给留 `[待确认]`。
- data/ 唯一写者 = ingest.py；formula_state.json 唯一写者 = formula_runner.py。

## 5. 验收标准

1. 回归测试：formula_runner 归一化（工业矿/中文类别/复合类别 anomaly）、build_output 三门（各含正/负例）。
2. 复测线程：交付文件名规范、toc 全覆盖、每三级节 ≥3 句、[待确认] 覆盖所有缺失槽、formula_state 无手改痕迹（全部槽位带 source）。
3. 量化：有效字符 ≥30,000（深度门+范文范式后预期），toc 覆盖 100%。

## 6. 遗留（不在本次范围）

- 倾角/海拔类叙述数字的来源校验属 consistency CC 类人工项，维持现状+汇报列出。
- bid-proposal-writing 同类单薄问题另线处理。
- KF 地质语料库：多矿种/多阶段规模化时再立项。
